{
  "builds": [
    {
      "src": "dist/cross-creek-creates/server/server.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/cross-creek-creates/server/server.mjs" }
  ]
}